header:
  version: 8
  includes:
    - kas/qbee-cos/scarthgap.common.yml
machine: qemux86-64

target:
  - qemu-demo-bundle

local_conf_header:
  qemux86_64: |
    MACHINE_FEATURES:append = " pcbios efi"
    EXTRA_IMAGEDEPENDS += "ovmf"